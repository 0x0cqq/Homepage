{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "name": "Preview Homepage",
            "request": "launch",
            "file": "${workspaceFolder}/Homepage/index.html",
            "webRoot": "${workspaceFolder}/Homepage"
        }
    ]
}